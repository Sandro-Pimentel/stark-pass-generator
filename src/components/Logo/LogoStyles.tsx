import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  title: {
    textAlign: 'center',
    fontWeight: 'bold',
    fontSize: 30,
    color: '#FFE31F',
  },
  image: {
    resizeMode: 'contain',
    height: 200,
  }
});
