import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  input: {
    width: '100%',
    height: 70,
    backgroundColor: '#FFE521',
    color: 'black',
    fontSize: 35,
    fontWeight: 'bold',
    borderWidth: 1,
    borderRadius: 3,
    padding: 10,
    textAlign: 'center',
    marginBottom: 40,
  }
});
